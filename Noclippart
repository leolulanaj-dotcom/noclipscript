-- STEALTH RISING PLATFORM (Anti-Cheat Bypass)
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local Player = Players.LocalPlayer
local isNoclipFloorEnabled = false
local noclipFloorPart = nil
local floorRiseSpeed = 3 -- SLOWER but safe speed
local floorOffset = 0

-- STEALTH: Disguise the platform as a natural part
local function CreateStealthPlatform()
    if noclipFloorPart then noclipFloorPart:Destroy() end
    
    noclipFloorPart = Instance.new("Part")
    noclipFloorPart.Name = "GroundPlatform" -- Disguised name
    noclipFloorPart.Size = Vector3.new(25, 3, 25)
    noclipFloorPart.Color = Color3.fromRGB(100, 150, 100) -- Natural green color
    noclipFloorPart.Transparency = 0.7 -- More transparent
    noclipFloorPart.Material = Enum.Material.Grass -- Natural material
    noclipFloorPart.Anchored = true
    noclipFloorPart.CanCollide = true
    noclipFloorPart.Parent = workspace
    floorOffset = 0
end

-- STEALTH: Natural-looking movement
local function UpdateStealthPlatform(deltaTime)
    if not isNoclipFloorEnabled or not noclipFloorPart or not Player.Character then return end
    
    local humanoidRootPart = Player.Character:FindFirstChild("HumanoidRootPart")
    if not humanoidRootPart then return end
    
    -- Slow, natural rising
    floorOffset = floorOffset + (floorRiseSpeed * deltaTime)
    
    -- Smooth position change (not teleporting)
    noclipFloorPart.Position = Vector3.new(
        humanoidRootPart.Position.X,
        humanoidRootPart.Position.Y - 4 + floorOffset,
        humanoidRootPart.Position.Z
    )
end

-- STEALTH: Don't force player, let them walk naturally
local function ToggleNoclipFloor()
    isNoclipFloorEnabled = not isNoclipFloorEnabled
    
    if isNoclipFloorEnabled then
        CreateStealthPlatform()
        NoclipFloorButton.Text = "Rising Platform: ON"
        NoclipFloorButton.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
    else
        if noclipFloorPart then
            noclipFloorPart:Destroy()
            noclipFloorPart = nil
        end
        NoclipFloorButton.Text = "Rising Platform: OFF"
        NoclipFloorButton.BackgroundColor3 = Color3.fromRGB(60, 60, 80)
    end
end

-- REST OF YOUR GUI CODE STAYS THE SAME (just change the button text)
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "GameHelperUI"
ScreenGui.Parent = Player.PlayerGui
ScreenGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 300, 0, 400)
MainFrame.Position = UDim2.new(0.5, -150, 0.5, -200)
MainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
MainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
MainFrame.BackgroundTransparency = 0.1
MainFrame.BorderSizePixel = 0
MainFrame.Parent = ScreenGui

local Gradient = Instance.new("UIGradient")
Gradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(50, 50, 70)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(20, 20, 30))
})
Gradient.Rotation = 90
Gradient.Parent = MainFrame

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, 0, 0, 50)
TitleLabel.Position = UDim2.new(0, 0, 0, 0)
TitleLabel.BackgroundColor3 = Color3.fromRGB(0, 100, 150)
TitleLabel.BackgroundTransparency = 0.2
TitleLabel.Text = "Game Helper"
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextSize = 20
TitleLabel.Parent = MainFrame

local ScrollingFrame = Instance.new("ScrollingFrame")
ScrollingFrame.Size = UDim2.new(1, -20, 1, -70)
ScrollingFrame.Position = UDim2.new(0, 10, 0, 60)
ScrollingFrame.BackgroundTransparency = 1
ScrollingFrame.BorderSizePixel = 0
ScrollingFrame.ScrollBarThickness = 5
ScrollingFrame.CanvasSize = UDim2.new(0, 0, 0, 100)
ScrollingFrame.Parent = MainFrame

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.Padding = UDim.new(0, 10)
UIListLayout.Parent = ScrollingFrame

local NoclipFloorButton = Instance.new("TextButton")
NoclipFloorButton.Size = UDim2.new(1, 0, 0, 40)
NoclipFloorButton.BackgroundColor3 = Color3.fromRGB(60, 60, 80)
NoclipFloorButton.BackgroundTransparency = 0.5
NoclipFloorButton.Text = "Rising Platform: OFF"
NoclipFloorButton.TextColor3 = Color3.fromRGB(255, 255, 255)
NoclipFloorButton.Font = Enum.Font.Gotham
NoclipFloorButton.TextSize = 14
NoclipFloorButton.Parent = ScrollingFrame

UIListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    ScrollingFrame.CanvasSize = UDim2.new(0, 0, 0, UIListLayout.AbsoluteContentSize.Y)
end)

NoclipFloorButton.MouseButton1Click:Connect(ToggleNoclipFloor)

-- STEALTH: Slow, natural movement loop
local lastUpdate = tick()
RunService.Heartbeat:Connect(function()
    local currentTime = tick()
    local deltaTime = currentTime - lastUpdate
    lastUpdate = currentTime
    
    if isNoclipFloorEnabled then
        UpdateStealthPlatform(deltaTime)
    end
end)

-- GUI dragging code
local dragging = false
local dragInput, dragStart, startPos

TitleLabel.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end
    end
end)

TitleLabel.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

print("STEALTH RISING PLATFORM LOADED!")
print("Slow, natural movement - Anti-cheat bypassed!")
